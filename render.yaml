services:
  - type: web
    name: vnindex-web
    runtime: python
    plan: free
    rootDir: .
    buildCommand: pip install uv && uv sync
    startCommand: uv run uvicorn web_app:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
